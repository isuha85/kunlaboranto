---
layout: default
title: 1951-04-18-MEMO.md
---

### 기억

(-) 1951-04-18 감사!!

### 메모

```TEXT

--> 160p ==>206 / 159 ==> 230  

-- 1차완료
기본적인 SELECT 명령문 작성
INDEX를 사용하는 SQL
날짜 함수 활용
TOP-n 질의 활용

-- 2차완료
조인, 서브쿼리 활용
WITH 절
그룹 함수 활용

-- 진행예정
분석 함수 활용
계층 질의 활용
정규식 (Regular Expression) 


-- 그런데 PG 에서는 순환을 오류처리하지 못하고, 무한루프, 강제로 TIMEOUT 설정후 테스트

  => SET statement_timeout = 10000 ; -- 일반유저 가능
  => -- SET temp_file_limit = '1GB'; -- Superuser 권한 필요 또는 설정전용 PSM 작성

  => .. 수행SQL은 후속 기술 ..

-- (특이점-1) TIMEOUT 이 "10초" 설정인데, 12초 소요됨, 추가 2초는 자원정리 소요 시간으로 추정됨 
-- (특이점-2) 해당 시점에 메모리 사용량 급증 관측, 아래와 같은 장애 방지 설정을 검토필요.
	--          # ALTER ROLE 유저명 SET statement_timeout = 3600000 ;
	--          # ALTER ROLE 유저명 SET temp_file_limit = '1GB'    ;
CREATE DATABASE test WITH OWNER postgres
    TEMPLATE template0
    LC_COLLATE 'C' -- [필수] 'C.UTF-8' 등의 설정인 경우 LIKE 가 인덱스를 이용할 수 없음
    LC_CTYPE 'C'   -- 필요시 UTF8도 가능 ( 'C' , ' ko_KR.UTF-8' )
    ENCODING 'UTF8'
    TABLESPACE ts_test
;

-- 임시방편으로 인덱스 생성시 "LIKE 최적화 인덱스 옵션 (TEXT_PATTERN_OPS 등) 을 추가하여 회피할 수 있으나, 해당 옵션으로 인덱스 생성시에는 부등호(대소비교)가 인덱스를 이용할 수 없다. 가능하다면 DB를 재생성하는 것을 권장한다.


```